The validation stance:
extras pass, growth is never rejected.

# Properties

## Extras pass
A field outside the [[type effective shape]] still validates.
See [[type extras]].

## Advisory stream
Out-of-shape fields surface as advisory output, not errors.

## Stricter gates layer on top
A pre-publish gate wanting rejection implements it as separate tooling.

Against the same advisory stream,
not in the validator.
